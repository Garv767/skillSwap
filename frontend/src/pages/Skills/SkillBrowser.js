import React from 'react';
import { Container, Typography } from '@mui/material';

const SkillBrowser = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4">Skill Browser</Typography>
    <Typography variant="body1" sx={{ mt: 2 }}>Browse skills functionality coming soon...</Typography>
  </Container>
);

export default SkillBrowser;